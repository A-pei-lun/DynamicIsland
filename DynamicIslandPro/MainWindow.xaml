<Window x:Class="DynamicIslandPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DynamicIslandPro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DynamicIsland Pro"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        Topmost="True"
        Width="200"
        Height="40"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">

    <Window.ContextMenu>
        <ContextMenu Opened="OnContextMenuOpened" Closed="OnContextMenuClosed">
            <MenuItem Header="强制展开" Click="ForceExpand_Click"/>
            <Separator/>
            <MenuItem Header="设置…" Click="OpenSettings_Click"/>
            <MenuItem Header="测试提醒" Click="TestAlert_Click"/>
            <MenuItem Header="测试打断" Click="TestInterrupt_Click"/>
            <MenuItem Header="测试动作提醒" Click="TestActionAlert_Click"/>
            <Separator/>
            <MenuItem Header="退出" Click="Exit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border x:Name="GlassBorder"
            CornerRadius="20"
            BorderThickness="0.5"
            Background="#BF1A1A1A"
            BorderBrush="#33FFFFFF">
        <Grid x:Name="RootGrid">
            <!-- 液态玻璃自渲染层（仅 LiquidGlass 模式显示，由 HlslGlassBackend 驱动）：
                 GlassCaptureH=Image 挂 H-pass 着色器（水平高斯），Source 喂 BitBlt 抓屏；
                 GlassCapture=Rectangle 挂 V-pass 着色器（垂直高斯），Fill=VisualBrush(GlassCaptureH)。
                 两遍链式合成，全程 GPU（ps_3_0）。 -->
            <Image x:Name="GlassCaptureH" Stretch="Fill" Visibility="Collapsed"/>
            <Rectangle x:Name="GlassCapture" Visibility="Collapsed"/>
            <Border x:Name="GlassTint" Background="Transparent" Visibility="Collapsed"/>

            <!-- 顶部高光（初始 Opacity 由 code-behind 的 HighlightBaseOpacity 设定，hover 时增亮） -->
            <Border x:Name="TopHighlight"
                    Height="16"
                    VerticalAlignment="Top"
                    Margin="8,2,8,0"
                    CornerRadius="10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#44FFFFFF" Offset="0"/>
                        <GradientStop Color="#00FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- 底部微光 -->
            <Border x:Name="BottomHighlight"
                    Height="10"
                    VerticalAlignment="Bottom"
                    Margin="8,0,8,2"
                    CornerRadius="5">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                        <GradientStop Color="#22FFFFFF" Offset="0"/>
                        <GradientStop Color="#00FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- 状态文字（收起/悬停态）。用 MarqueeText：长歌名超出可视宽时自动横向滚动。
                 MaxWidth 由 code-behind 按当前状态宽动态设定，给溢出判定一个有限基准。 -->
            <local:MarqueeText x:Name="StatusText"
                       Text="🕒 --:--"
                       Foreground="White"
                       FontSize="14"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />

            <!-- 展开态自定义视图宿主（仅当 source 提供 ExpandedView 时显示） -->
            <ContentControl x:Name="ExpandedHost"
                            Visibility="Collapsed"
                            Focusable="False"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"/>

            <!-- 瞬时提醒宿主：alert 激活时显示 AlertView，优先级高于 ExpandedHost/StatusText -->
            <ContentControl x:Name="AlertSlot"
                            Visibility="Collapsed"
                            Focusable="False"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
